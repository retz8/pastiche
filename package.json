{
  "name": "pastiche",
  "version": "0.0.0-dev",
  "private": true,
  "type": "module",
  "description": "Faithful design-system execution for LLM coding agents",
  "scripts": {
    "test": "node --test --import tsx 'scripts/*.test.ts'",
    "extract:fact": "node --import tsx scripts/extract-fact-ts.ts",
    "test:lint": "cargo test --manifest-path rust/pastiche-lint/Cargo.toml"
  },
  "devDependencies": {
    "ts-morph": "^25.0.1",
    "tsx": "^4.20.0",
    "typescript": "^5.7.0",
    "yaml": "^2.7.0",
    "@types/node": "^22.10.0"
  }
}
